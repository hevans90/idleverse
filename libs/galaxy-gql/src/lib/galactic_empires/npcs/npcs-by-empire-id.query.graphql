query NpcsByEmpireId($id: uuid!) {
  galactic_empire_npc(where: { galactic_empire_id: { _eq: $id } }) {
    npc {
      name
      image_url
      playable_race {
        name
        id
        image_url
      }
      faction {
        image_url
        id
        name
      }
    }
  }
}
