table:
  schema: public
  name: quest_reward
insert_permissions:
  - role: dev
    permission:
      check: {}
      columns:
        - quest_id
        - type
select_permissions:
  - role: dev
    permission:
      columns:
        - type
        - id
        - quest_id
      filter: {}
  - role: user
    permission:
      columns:
        - type
        - id
        - quest_id
      filter: {}
