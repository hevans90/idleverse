table:
  schema: public
  name: planetary_ring
select_permissions:
  - role: user
    permission:
      columns:
        - colors
        - rotation
        - terrain_bias
        - resolution
        - inner_radius
        - outer_radius
        - type
        - id
      filter: {}
