table:
  name: planetary_ring
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - colors
        - rotation
        - terrain_bias
        - resolution
        - inner_radius
        - outer_radius
        - type
        - id
      filter: {}
