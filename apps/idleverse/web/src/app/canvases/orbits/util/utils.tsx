import { Planet } from '../planets/planet';

export const calcuateGravity = (
  body: { x: number; y: number },
  planet: Planet
) => {};

export const calculateTotalGravity = (
  pos: { x: number; y: number },
  bodies: Planet[]
) => {};
