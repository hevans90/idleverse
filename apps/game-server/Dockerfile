# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build idleverse-game-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t idleverse-game-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system idleverse-game-server && \
          adduser --system -G idleverse-game-server idleverse-game-server

COPY dist/apps/game-server idleverse-game-server
RUN chown -R idleverse-game-server:idleverse-game-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix idleverse-game-server --omit=dev -f install

CMD [ "node", "idleverse-game-server" ]
